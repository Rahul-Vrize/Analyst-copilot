beautifulsoup4>=4.12
lxml>=5.0
duckdb>=1.0
pydantic>=2.6
python-dotenv>=1.0
streamlit>=1.35
anthropic>=0.34
boto3>=1.34

# Optional (Phase 2 - Inline XBRL fact extraction). Install separately if needed:
# arelle-release>=2.20
